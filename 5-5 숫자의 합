#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>

int main() {
	int n;
	char arr[100];
	int sum = 0;
	scanf("%d", &n);
	scanf("%s", arr);
	
	for (int i = 0; i < n; i++) {
		sum += arr[i] - '0'; // 아스키 코드 값으로 숫자를 나타내기 위해 '0'= 48을 빼줌
	}
	printf("%d", sum);
	return 0;
}
