#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
	int n = 0;
	scanf("%d", &n);
	for (int i = 0; i < n; i++)
	{
		for (int j = n; j>0; --j)
		{
			if (j <= i+1) printf("*", j);
			else printf(" ");
		}
		printf("\n");
	}
	return 0;
}
